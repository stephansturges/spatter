[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "overlayaug"
version = "0.1.0"
description = "Occlusion-aware overlay augmentation for PyTorch detection training."
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "OverlayAug Contributors"}]
license = {text = "MIT"}
dependencies = [
  "numpy>=1.23",
  "opencv-python-headless>=4.8",
  "pyyaml>=6.0",
]

[project.optional-dependencies]
torch = ["torch>=2.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100

[tool.pytest.ini_options]
addopts = "-q"
